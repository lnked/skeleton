;(function ($) {
	"use strict";

	$.app = $.app || {};

	$.app.plugin = {

		init: function()
		{

		}

	};

})(jQuery);

// ;(function (window) {
// 	"use strict";

// 	var app = window.app = window.app || {};

// 	app.browser = {
// 		name: 'Chrome',

// 		getUrl: function (url) {
// 			return chrome.extension.getURL(url);
// 		}
// 	};

// })(window);